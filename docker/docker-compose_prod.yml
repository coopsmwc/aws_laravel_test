version: "3"
services:
  web:
    image: blueminboy/alpine-web
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: "0.1"
          memory: 50M
      restart_policy:
        condition: on-failure
    ports:
      - "4000:80"
    networks:
      - webnet
  api:
    image: blueminboy/alpine-api
    deploy:
      replicas: 2
      resources:
        limits:
          cpus: "0.1"
          memory: 50M
      restart_policy:
        condition: on-failure
    ports:
      - "4001:80"
    networks:
      - webnet
  mysql:
    image: mysql:5.7
    volumes:
       - /Users/adminmwc/dev/home/www/docker-data/mysql:/var/lib/mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    networks:
      - webnet
networks:
  webnet:
